import { KeyTransformPipe } from './key-transform.pipe';

describe('KeyTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
